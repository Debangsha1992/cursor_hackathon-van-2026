# Copy this file to `.env.local` and fill in real values from the
# Supabase dashboard: Project Settings -> API. The auth pages render
# without these (placeholders are used) but submitting the forms will
# only succeed once they are set.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
